import './App.css';

// todo - extended narrative format
//  - AWAIT PLAYER PROMPT (continue...)?
//  - time before showing the message
//  - reading speed (typewriter css animation)
// todo - adjust/remove reading speed (typewriter css animation) as user preference
// todo - how to handle changing scene description? (e.g. once some condition satisfied, it's different)
// todo - fixed set of possible interaction commands for all objects OR list interactions?
// todo - navigation from within objects interaction (e.g. door) OR separate property of a scene
//  - maybe combined?
// todo - how to handle the conditions?
//  - global list of objects/entities etc.? (most likely)
//  - access properties of scenes?
//  - how to encode in the JSON?

function App() {
  return (
    <div className="App">
      Foo
    </div>
  );
}

export default App;
